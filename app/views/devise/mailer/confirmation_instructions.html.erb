<p><%= t('.greeting', :recipient => @resource.email, :default => "Welcome to PrimalTV, #{@resource.email}!") %></p>

<p><%= t('.instruction', :default => "You can confirm your account email through the link below. Once confirmed you're ready to go! Enjoy 🎉") %></p>

<p><%= link_to t('.action', :default => "Confirm my account"),
         confirmation_url(@resource, :confirmation_token => @token, locale: I18n.locale) %></p>
